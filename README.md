# 苏华食养状态观察卡

上传一张正面照片并选择近期生活状态，生成一张非医疗的食养生活观察卡。照片仅用于生成视觉卡片，建议来自用户主动选择的信息，不从面部推断健康状况。

## 本地运行

1. 复制 `.env.example` 为 `.env.local`，填写服务端 `XIAOJI_API_KEY`。
2. 运行 `npm install`。
3. 运行 `npm run dev`。

前端：http://localhost:5173

